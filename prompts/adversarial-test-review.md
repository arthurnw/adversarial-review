---
description: Adversarially pressure-test tests (esp. LLM-generated) — 3 reviewers, 3 challengers, then per-test findings with concrete fixes
argument-hint: "[ref | range | test-path...]"
---

Use the `adversarial-test-review` skill. Read its `SKILL.md`, follow the
harness-detection step (which will route you to `dispatch-pi.md` in this
environment), then execute the full six-agent workflow.

Arguments to pass through to the skill: $@
